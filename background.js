chrome.storage.sync.get(function_l, function (data) {
    const isFunction1s = data.function1s !== undefined ? data.function1s : false;
    const isFunction2s = data.function2s !== undefined ? data.function2s : false;
    const isFunction3s = data.function3s !== undefined ? data.function3s : false;
  
    if (isFunction1s){
        //処理
    }

    if (isFunction2s){
        //処理
    }

    if(isFunction3s){
      //処理
    }



});